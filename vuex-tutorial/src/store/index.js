import { createStore } from 'vuex';
import todos from './modules/todos';

// Create store
export default createStore({
    state: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        todos
    }
});